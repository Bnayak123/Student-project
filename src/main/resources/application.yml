spring:
  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    password: Admin
    url: jdbc:mysql://localhost:3306/Student-Database?createDatabaseIfNotExist=true
    username: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true  
